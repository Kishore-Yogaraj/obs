### My solution
```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        # How do we remove all non-alphaneumeric characters?
        s = s.lower()
        result = ''
        for char in s:
            if char.isalnum():
                result += char
        
        L = 0
        R = len(result) - 1

        while L < R:
            if result[L] != result[R]:
                return False
            else:
                L +=1
                R -=1
        return True


```

### Actual Solution
This solution does the same as my solution but instead of creating the string we want first (one with only alphanumeric characters and no uppercase letters) we do it inside our while loops

```python
def isPalindrome(s):
	L = 0
	R = len(s) -1

	while L < R:
		while L < R and not s[L].isalnum():
			L += 1
		while R > L and not r[L].isalnum():
			R -= 1
		if s[L].lower() != s[R].lower():
			return False
		L += 1
		R -= 1
	return True
		
```

### Custom isalnum() function

```python
def alphaNum(self, c):
	return (ord('A') <= ord(c) <= ord('Z') or 
			ord('a') <= ord(c) <= ord('z') or 
			ord('0') <= ord(c) <= ord('9'))
```