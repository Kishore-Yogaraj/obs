### My Initial solution with hashmap

```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        # This is a dynamic sliding window question 
        # We can start off at the begining of the list and iterate through to create a window of any size
        # While iterating, we need to keep track of the length of the window

        # When we iterate through the array, the length should continuosly be updated as we add values to it
        # But what is the condition for when we add values: If nums[L] = nuns[R] then L = R

        # Let's initialize some values
        L = 0
        length = 0
        hashMap = {}

        # Iterate through the array with one pointer
        for R in range(len(s)):
            if s[R] in hashMap:
                hashMap[s[R]] += 1
                L = R
                hashMap = {}
                hashMap[s[R]] = 1
            else:
                hashMap[s[R]] = 1
            print(R-L+1)
            length = max(length, R - L + 1)
        return length
```

### This important to know for hashmaps
How do we add characters to a hashmap and keep track of how many characters we have seen so far?

```python
def count_characters(s: str) -> dict:
    # Initialize an empty dictionary to store character counts
    char_count = {}
    
    # Iterate through each character in the string
    for char in s:
        # If we've seen this character before, increment its count
        # If we haven't seen it, initialize its count to 1
        if char in char_count:
            char_count[char] += 1
        else:
            char_count[char] = 1
    
    return char_count
```

### Things we struggled with
We got close using the max window approach we learned earlier but there are a lot of edge cases:
- What happens when you see a duplicate and the left pointer is not currently on that duplicate?
- What happens if a letter in between a duplicate leads to a substring without repeating characters?
	- You would essentially skip the letter as a whole

### How do we think about the solution
We need to think about making the window bigger and smaller based on whether we have a duplicate or not. 

So we check the first index, then the second, and then the third and on the 4th we notice that there is a duplicate. Now we need to make our window smaller to remove the duplicate.

Now we have to ask ourself this question: **How would we know instantly if we have a duplicate or not?**

We can use a **set** because sets can't contain duplicates. Now we get stuck here. How exactly do we use a set to ensure we end up with no duplicates?

If we are working with sets we are most likely adding and removing characters. The set doesn't mean duplicates are handled automatically, all a set allows us to do is to remove based on the value rather than an index. That is what is meant by being able to detect a duplicate **instantly**

So the process is to iterate with the right pointer.

While there is a duplicate present in the subarray, remove characters from the far left.

**And then we can add that character back to the set. All the set does is allow us to hold significant characters within in and use it as a condition to manipulate our pointers.**

```
while s[R] in window:
	window.remove(s[L])
	L += 1
window.add(s[R])
length = max(lenght, R-L+1)

```

```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        charSet = set()
        l = 0
        res = 0

        for r in range(len(s)):
            while s[r] in charSet:
                charSet.remove(s[l])
                l += 1
            charSet.add(s[r])
            res = max(res, r - l + 1)
        return res

```