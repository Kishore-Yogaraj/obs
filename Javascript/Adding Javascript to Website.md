HTML has an element called the script element which allows for javascript code to be written
- The script element is typically written at the bottom of the body element
- We do this because we want the web page to be created first and then we use javascript to make it interactive

You can also run javascript code inside html using **attributes**
- An example of an attribute being used in this scenario is the "onclick" method which will perform a function when a button is clicked